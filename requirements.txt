# AI Universe Swarm Engine — cloud-safe dependency matrix
# Hosts without a Rust toolchain must not compile tiktoken from source.
# Install: pip install -r requirements.txt

--only-binary :all:

fastapi==0.111.0
uvicorn==0.30.1
crewai==0.28.8
crewai-tools==0.1.6
pydantic==2.7.1
langchain==0.1.16
langchain-core==0.1.42
litellm==1.35.32